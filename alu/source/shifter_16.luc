module shifter16 (
    input alufn[6],  // OPCODE
    input x[16],     // X
    input y[16],     // Y
    output out[16]   // Output 
  ) {

  always {
  
    case(alufn[1:0]) {
      
      // For shift left
      b00: 
        out = x << y;
        
      // For shift right
      b01: 
        out = x >> y;
        
      // For shift right arithmetic  
      b11: 
        out = $signed(x) >>> $signed(y);
      
      default:
        out = x;
    }
  }
}